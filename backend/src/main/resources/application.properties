spring.application.name=backend

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

spring.datasource.url=jdbc:mysql://shortline.proxy.rlwy.net:31577/smartcampus?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=ZixQAKGzcMgcArgEoVLPHrKBxIkFFCnX
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Session cookie for SPA (inactive session ends after 30 minutes)
server.servlet.session.timeout=30m
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.same-site=lax

# Google OAuth2 (set via environment variables)
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:placeholder-client-id}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET:placeholder-client-secret}
spring.security.oauth2.client.registration.google.scope=openid,profile,email

# --- Incident reply emails (optional) ---
# Without spring.mail.host, reply emails are logged only (see EmailNotificationService).
# Uncomment and set for real SMTP (e.g. Gmail app password, SendGrid, etc.):
# spring.mail.host=smtp.gmail.com
# spring.mail.port=587
# spring.mail.username=your-sender@gmail.com
# spring.mail.password=your-app-password
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true
# spring.mail.from=your-sender@gmail.com
